class Task {
  int id;
  String title;
  String description;
  bool isCompleted;

  Task({
    this.id = 0,
    this.title = "Unnamed task",
    this.description = "no description",
    this.isCompleted = false,
  });
}
