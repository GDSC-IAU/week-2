class Task {
  int id;
  String title;
  String description;
  bool isCompleted;

  //Task(this.id, this.title, this.description, this.isCompleted);

  Task({
     this.id = 0,
     this.title = "",
     this.description = "",
     this.isCompleted = false,
   });
}